# OPEN GAPS

- Decide whether this repo should stay a combined flowref + ETNF checkout or be trimmed to the ETNF tool only.
- If trimming: keep `Etnf.lean`, `lakefile.lean`, fixture data, tests needed for `flowref-etnf`, and remove unrelated decompiler docs/code in a separate verified step.
- If keeping combined: update package naming so `flowref-etnf` is the obvious public entry point.
